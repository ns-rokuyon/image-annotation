<nav class="navbar navbar-inverse navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="<%= route_link('') %>"><%= settings.app_name %></a>
        </div>
        <ul class="nav navbar-nav">
            <% if settings.enable_module['listview'] %>
                <li><a href="<%= route_link('list') %>">List</a></li>
            <% end %>
            <% if settings.enable_module['compare'] %>
                <li><a href="<%= route_link('compare') %>">Compare</a></li>
            <% end %>
        </ul>
    </div>
</nav>
